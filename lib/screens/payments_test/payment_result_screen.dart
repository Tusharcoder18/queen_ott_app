import 'package:flutter/material.dart';

class PaymentsResultsScreen extends StatelessWidget {
  const PaymentsResultsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Payments Results Page,"),
      ),
    );
  }
}
